name: Atomic release

on:
  push:
    branches: [develop]

jobs:
  release:
    uses: lornu-ai/atomic-rollout/.github/workflows/release.yml@main
    with:
      config_path: rollout.yaml
    secrets: inherit
